{
  "name": "selection-launcher",
  "version": "0.1.5",
  "private": true,
  "license": "MIT",
  "description": "Keyboard-first actions for selected text in Chromium browsers.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run check && npm test && node scripts/build.mjs",
    "test": "node --test tests/shared.test.js tests/publish.test.mjs tests/manifest.test.mjs tests/content.test.mjs",
    "check": "node --check src/shared.js && node --check src/background.js && node --check src/content.js && node --check src/options.js && node --check scripts/build.mjs && node --check scripts/publish.mjs",
    "release": "node scripts/publish.mjs"
  }
}
